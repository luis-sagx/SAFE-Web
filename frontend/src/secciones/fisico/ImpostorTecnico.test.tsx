import { describe, expect, it } from 'vitest'
import ImpostorTecnico from './ImpostorTecnico'

describe('ImpostorTecnico', () => {
  it('componente existe y se puede importar', () => {
    expect(ImpostorTecnico).toBeDefined()
  })
})
